<h2>Denuncias</h2>
<% if (!reportsList.length) { %>
  <p>No existen Denuncias</p>

<% } else { %>
  <table id="reports-list">
    <thead>
      <tr>
        <th>Título</th>
      </tr>
    </thead>
    <tbody>
      <% reportsList.forEach((report) => { %>
        <tr>
          <td><a href="<%= showReportPath(report)%>"><%= report.title %></a></td>
        </tr>
      <% }) %>
    </tbody>
  </table>
<% } %>