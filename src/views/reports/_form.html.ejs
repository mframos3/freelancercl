<div>
  <% if (locals.errors) { %>
    <div class="errors">
      TIenes que solucionar los siguientes errores:
      <ul>
        <% errors.forEach(error => { %>
          <li><%= error.message %></li>
        <% }); %>
      </ul>
    </div>
  <% } %>
  <form class="form-style-1" action="<%= submitReportPath %>" method="post">
    <% if (!report.isNewRecord) { %>
      <input type="hidden" name="_method" value="patch" />
    <% } %>
    <div  class="field">
      <label for="title">Título</label>
      <input type="text" name="title" value="<%= report.title %>" />
    </div>
    <div  class="field">
      <label for="content">Contenido</label>
      <input type="text" name="content" value="<%= report.content %>" />
    </div>
    <div  class="field">
      <input type="hidden" name="reportingUserId" value="<%=currentUser.id%>"></input>
    </div>
    <div  class="field">
      <input type="hidden" name="reportedUserId" value="<%= postUserId %>"></input>
    </div>
    <div  class="field">
      <input type="hidden" name="reportedPost" value="<%= postId %>"></input>
    </div>
    <div  class="field">
      <% if (report.isNewRecord) { %>
        <input type="submit" name="create" value="Create" />
      <% } else { %>
        <input type="submit" name="update" value="Update" />
      <% } %>
    </div>
  </form>
</div>